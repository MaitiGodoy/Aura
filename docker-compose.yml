version: '3.8'
services:
  aura-frontend:
    build: .
    ports:
      - "80:80"
    restart: always
    environment:
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"